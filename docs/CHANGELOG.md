# CHANGELOG — v2.1.0 (إعادة الهيكلة والتأمين والتسريع)

## السبب الجذري لصفحة "عذراً، حدث خطأ مؤقت" المتكررة (أُعيد إنتاجه واختُبر)
عشرات الصفحات والمكتبات تستدعي `await ensureTable(env)` / `ensureAiTables(env)` بشكل دفاعي في كل طلب. عندما يكون ترحيل المخطط معلّقاً (بعد نشر جديد، أو بعد قاعدة ناقصة) يكون علم الـisolate ما زال `false` فتنفّذ هذه الدوال **كل أوامر DDL (مئات نداءات D1)** داخل طلب الصفحة، فيتجاوز الطلب حد الـ50 subrequest في الخطة المجانية ويرمي استثناء ← صفحة الخطأ الاحتياطية. على قاعدة جديدة وبحد 50 نداء/طلب: النسخة القديمة أعطت `500,500,500,200,200` والجديدة `200` في كل الطلبات الـ14.
الإصلاح: (1) `ensureTable/ensureAiTables/ensureAccountTables` لا تفعل شيئاً خارج الترحيل المُدار (`ensureAllSchema` وحده يملك التهيئة)؛ (2) ميزانية الترحيل داخل طلب صفحة خُفّضت من 35 إلى 18 نداء لأنها تتشارك الحد مع رسم الصفحة؛ (3) الـCron وزر `Repair schema` في `/admin/system` يعملان بميزانية 44 لأنهما لا يرسمان صفحة؛ (4) فشل `ensureAllSchema` لم يعد يُسقط الموقع كله (يُسجَّل ويستمر الطلب).

## تعذّر دخول لوحة التحكم رغم صحة كلمة المرور (أُعيد إنتاجه)
محدّد المعدّل (5 محاولات/15 دقيقة) كان **يفشل مغلقاً**: إن لم يكن جدول `rate_limits` جاهزاً بعد (ترحيل معلّق) يُرفض الدخول بحالة 429 لكن الصفحة تعرض "Incorrect password" — وهي رسالة مضلِّلة. أيضاً كل محاولة، حتى الصحيحة، كانت تُحسب ضمن الحد. الإصلاح: محدّد احتياطي في الذاكرة عند تعذّر D1، رسالة دقيقة ("Too many attempts. Try again in N minute(s)")، وتصفير العدّاد عند نجاح الدخول.

## اختفاء الوظائف من الموقع
الموقع لا يعرض إلا `status = 'active'`. دورة حياة الوظائف (`db/cleanup.js`) تحوّل أي وظيفة لم تُحدَّث منذ 30 يوماً إلى expired ثم archived ثم تحذفها نهائياً؛ وإن توقفت المزامنة (أعطال الموقع، حصة D1) تبدو كل الوظائف "قديمة" في آن واحد. أُضيفت **صمّامات أمان**: لا تُنفَّذ دورة الحياة ما لم تنجح مزامنة خلال آخر 72 ساعة، ولا يُنهى أكثر من 50% من كتالوج كبير في تشغيل واحد. وفي `/admin/system` بطاقة **Jobs by status** وزر **Reactivate N hidden jobs** لاستعادة الوظائف المخفية فوراً. وخُفّضت 40 استعلاماً متوازياً في صفحة النظام إلى 2 (كانت تتجاوز حد 50 نداء).

## الوظائف تظهر بمؤشر تحميل دائم في الصفحة الرئيسية
استعلام قائمة الوظائف يذكر أعمدة لا توجد إلا بعد اكتمال ترحيل المخطط؛ كان أي فشل يُبتلع بصمت فتبقى الصفحة على spinner للأبد (وتُخزَّن هكذا 60 ثانية). الآن: 3 مستويات استعلام احتياطية، وكل خطوة إثراء (شعارات/HOT PAY) لها fallback مستقل، وإن تعذّر جلب القائمة تُعلَّم الصفحة `data-degraded` فلا تُخزَّن (`no-store`) ويجلبها المتصفح من `/api/jobs` مع مهلة 12 ثانية ورسالة واضحة بدل الدوران؛ وإذا لم توجد وظائف فعلاً تظهر حالة "No jobs yet" بدل الـspinner. اختبار انحدار: صفحة رئيسية تعرض الوظيفة طوال فترة الترحيل.

## أزمة "تجاوزتَ حد القراءات اليومي" (D1 free tier row-read limit) — السبب الجذري والحل الشامل
**هذا هو السبب الحقيقي خلف الصورتين اللتين أرسلتهما:** لوحة الأدمن رفضت الدخول، وكل الوظائف اختفت من الرئيسية — كلتاهما نتيجة رسالة واحدة: `D1_ERROR: exceeded free tier daily row read limit`.

خطة Cloudflare D1 المجانية تسمح بـ5,000,000 "صف مقروء" يومياً، وكل صف **يفحصه** أي استعلام يُحتسب — لا الصفوف المُعادة فقط. قِستُ التكلفة الفعلية بمحاكاة محلية على كتالوج من 20,000 وظيفة:
- **تحميل واحد للصفحة الرئيسية كان يقرأ نحو 70,000 صف** (عدّادات `COUNT(DISTINCT company)`، تجميع الفئات بـ`LIKE` لكل فئة، إلخ — كلها حية على كل زيارة).
- **صفحة `/companies` كانت تقرأ نحو 72,000 صف**: أربعة استعلامات فرعية مرتبطة (correlated subquery) لكل شركة معروضة، تُحسب لـ**كل** الشركات (855) قبل الترتيب حسب عدد الوظائف — وليس فقط للصفحة المعروضة.
- **لوحة `/admin` وحدها كانت تُطلق نحو 40 استعلاماً متوازياً** تقرأ مئات الآلاف من الصفوف مجتمعة.

عشرات زيارات كهذه كانت كافية لاستنفاد الحصة اليومية، وبعدها **يفشل كل استعلام في الموقع** — بما فيه تسجيل دخول الأدمن وعرض الوظائف، وهو تماماً ما ظهر في الصورتين.

### الحل: طبقة تجميع محسوبة مسبقاً
- **جدول `site_cache` جديد** + **`lib/platform/site-cache.js`**: مرور واحد على الوظائف النشطة (بترقيم مُتسلسل، محدود الحجم) يحسب معاً: الإجمالي، عدد الشركات، توزيع الفئات، توزيع المهارات، توزيع المواقع، نطاقات الرواتب، توزيع الحالة والمصدر ومستوى الراتب — كل ذلك في استعلام واحد مُجمَّع (batch)، يُكتب كصفوف JSON. يُحدَّث تلقائياً كل ~6.5 ساعة من الجدولة، أو فوراً بزر **Refresh stats** في `/admin/system`.
- **`lib/platform/job-window.js`**: أي استعلام لا يمكن أن يستخدم فهرساً (بحث `LIKE`، `json_each` للمهارات، فلاتر متعددة) يُقيَّد الآن بآخر 1,500–5,000 وظيفة نشطة فقط، بدل الجدول كاملاً — بغض النظر عن حجم الكتالوج مستقبلاً.
- **أعمدة جديدة على `companies`**: `job_count` و`remote_job_count` مُخزَّنتان (denormalized) ومُحدَّثتان أثناء التحديث الدوري، فصفحة الشركات تقرأ عمودين جاهزين بدل تشغيل 4 استعلامات فرعية لكل شركة.
- **فهارس جديدة على `jobs`**: تطابق ترتيب العرض الفعلي (`status, featured, id`) و(`featured, id`) لقائمة الأدمن غير المُصفّاة، وفهرس مطابق مطابقة تامة (لا `LOWER()`) على اسم الشركة بدل نمط `OR` الذي كان يدفع SQLite لتجاهل الفهرس.
- أعيد بناء: الرئيسية، صفحات الفئات/الشركات/الدول/المهارات/البحث، API الوظائف، ومعظم لوحات الأدمن (`/admin`, `/admin/jobs`, `/admin/system`, `/admin/sources`, `/admin/companies`, `/admin/analytics`) وحتى مولّد المدونة التلقائي — لتقرأ من هذه الطبقة بدل الاستعلامات الحية.
- **صمّام أمان في التنظيف اليومي** (`db/cleanup.js`) يبقى عمداً استعلاماً حياً (لا من الكاش): قرار حذف نسبة كبيرة من الوظائف يجب أن يعكس الواقع الحالي، لا رقماً قد يكون قديماً بساعات — فحص واحد يومي محدود السقف (20,000 صف) أهون بكثير من خطأ في قرار حذف جماعي.

### النتيجة المُقاسة (بنفس محاكاة 20,000 وظيفة)
| الصفحة | قبل | بعد |
|---|---|---|
| الرئيسية `/` | ~70,000 صف | 43–47 صف |
| `/companies` | ~72,000 صف | 10 صفوف |
| `/admin/system` | ~40,200 صف | 209 صفوف |
| مهمة التنظيف اليومية (Cron) | ~19,000 صف | 13 صفوف (عند تخطّي الدورة) |
| مولّد المدونة (Cron) | ~97,900 صف | ~25,500 صف |

بقيت بضع صفحات إدارية (نادرة الاستخدام) عند تكلفة أعلى نسبياً (`/admin/analytics`، بحث الأدمن بـ`LIKE`) لأن طبيعة الاستعلام (نطاق تاريخ يطابق معظم الجدول، أو بحث نصي حر) لا يمكن أن يُقيَّد دون فقدان الدقة — وهي صفحات أدمن فقط، لا كل زائر يضربها، فتُركت موثَّقة الحد كتنازل مقصود.

### درس مُستفاد أثناء هذا الإصلاح
أثناء العمل ارتكبتُ خطأ برمجياً محا معظم محتوى `src/db/cleanup.js` (خطأ في شرط ثلاثي بلغة بايثون أثناء تعديل تلقائي). اكتُشف فوراً عبر `node --check` والاختبارات (فشل الاستيراد بدل تمريره بصمت)، واستُعيد الملف من آخر نسخة ZIP ناجحة، وأُعيد تطبيق الإصلاح بعناية بعدها. أيضاً، الإصلاح الأول لصمّام أمان التنظيف استخدم رقماً **من الكاش** لعدد الوظائف النشطة — لكن اختبار الانحدار المُضاف حديثاً ضبط هذا: صمّام أمان يقرر حذفاً جماعياً يجب أن يقرأ رقماً **حياً** (ولو محدود السقف)، لا رقماً مخزَّناً قد يكون غير دقيق تماماً في تلك اللحظة تحديداً.

### اختبار انحدار جديد
`tests/smoke.test.mjs` يزرع الآن 3,000 وظيفة، يُحدِّث طبقة التجميع، ثم يتحقق أن الرئيسية وصفحات الدليل العامة تبقى ضمن حد معقول من نداءات D1 بغض النظر عن حجم الكتالوج — لمنع تكرار هذه الأزمة تحديداً مستقبلاً.

## إصلاحات أخطاء فعلية
| المشكلة | الملف | الإصلاح |
|---|---|---|
| الترحيل المتقطّع يتخطى جداول ثم يُسجّل اكتماله (22 من 57 جدولاً لم تُنشأ) بسبب أعلام `schemaEnsured` على مستوى الـisolate التي تغيّر مواضع الوحدات | `db/schema/*.js` | الأعلام تُتجاهل أثناء الترحيل (`env.__migCtx`)؛ رفع `SCHEMA_VERSION` إلى `2026-09-20.1` ليُعاد الترحيل ويُصلح القاعدة الحالية |
| أعلام الطلب على `env` قد تتسرّب بين الطلبات في نفس الـisolate | `index.js` | نسخة `env` لكل طلب |
| `/search/%E0%A4%A` ← HTTP 500 (`URIError`) | `lib/platform/search-utils.js`, `seo-pages.router.js` | فك ترميز آمن ← 404 |
| فشل مهام Cron يُبتلع بصمت (`.catch(() => {})`) | `app/cron.js` | يُسجَّل في `error_logs` + Workers Logs |
| CSS بطاقة الوظيفة مكرر في الصفحة الرئيسية ويختلف عن `JOB_CARD_CSS` | `pages/home/styles.js` | تستورد الصفحة الرئيسية الأنماط القانونية وحُذفت النسخة المكررة |

## أمان
- حذف `?jf_debug=<ADMIN_PASSWORD>`؛ التشخيص لمن سجّل الدخول عبر `?jf_debug=1`.
- `ADMIN_SESSION_SECRET` مستقل لتوقيع كوكي/CSRF الأدمن (يرجع إلى `ADMIN_PASSWORD`)، وفشل مغلق عند غياب أي سر.
- بوابة مصادقة مركزية تحت `/admin` (دفاع إضافي فوق فحص كل sub-router).
- معقّم HTML بقائمة سماح لأجسام المدونة والصفحات الثابتة (`lib/platform/html-sanitizer.js`).
- `/search/*` دائماً `noindex`؛ تهريب محارف LIKE؛ Rate limit للبحث؛ `robots.txt` يمنع مسارات الحسابات.
- CI: لا نشر قبل نجاح `scripts/audit.mjs` و`npm test`.

## أداء وتكلفة D1
- كاش حافة فعلي (Cache API) لصفحة الوظيفة و`/jobs` وصفحات الفئة/الشركة/الدولة/المهارة/البحث بمفاتيح مطبَّعة؛ الزائر المجهول يُحدَّد بكوكي الجلسة فقط.
- Rate limit للبحث يعمل عند فوات الكاش فقط (لا كتابة D1 عند الإصابة).
- تجميع أحداث التحليلات في beacon واحد بدل طلب لكل حدث؛ `/api/*` يُوجَّه مبكراً.
- `visits` تُسجَّل لصفحات 200 HTML فقط.
- الصفحة الرئيسية: 11 قراءة D1 بالتوازي بدل التتابع؛ إزالة بحث الكاش المكرر.
- 5 مهام Cron ← مهمة واحدة `*/30 * * * *` (موزّع في `app/cron.js`).
- `[observability]` مفعّلة في `wrangler.toml`.

## هيكلة
- `index.js` رفيع؛ استخراج `app/security-headers.js` و`app/fallback-error.js` و`app/cron.js`.
- `lib/` (46 ملفاً مسطحاً) ← 10 نطاقات: jobs · directory · companies · content · ai · analytics · seo · platform · accounts · monetization.
- `db/schema.js` (84KB) ← منسّق + `db/schema/` (5 ملفات). `db/analytics.js` ← `db/telemetry.js`.
- `routes/api.router.js` (44KB) ← موزّع + `routes/api/` (6 وحدات + shared).
- `pages/seo-pages.js` (83KB) ← barrel + `pages/seo/` (7 ملفات).
- `pages/home.js` (94KB) ← 364 سطراً + `pages/home/{styles,client-script}.js` (ناتج HTML مطابق بايت-ببايت عدا التعديلين المقصودين).
- تسميات أوضح: `lib/schema.js`→`lib/seo/jsonld.js`، `lib/seo.js`→`lib/seo/meta.js`، `analytics*`→`lib/analytics/{events,tracker,tag}.js`.

## تنظيف
- حذف: `lib/metadata.js`، `favicon-preview.png`، 17 استيراداً غير مستخدم، 19 رمزاً ميتاً. أُبقيت عمداً: مكتبة أيقونات Lucide، `PaymentService`، ثوابت المونيتايزيشن، بناة JSON-LD.
- نقل تقارير التدقيق إلى `docs/`.

## الهوية البصرية
- استبدال ~110 إيموجي بأيقونات Lucide (7 أيقونات جديدة في `assets/icons.js`). المتبقي بيانات يحرّرها الأدمن أو نص بريد إلكتروني.

## ملاحظات النشر
1. أول ~7 طلبات بعد النشر قد تُظهر "no such table" مؤقتاً أثناء إعادة الترحيل (تلقائي).
2. `wrangler secret put ADMIN_SESSION_SECRET` ← يُنهي جلسة الأدمن الحالية (تسجيل دخول واحد).
3. النشر يستبدل الـCron القديمة الخمسة بمهمة واحدة.
4. بعد النشر اضغط **Repair schema** في `/admin/system` مرة أو مرتين حتى تظهر رسالة "Schema is up to date" (يعالج القاعدة الحالية فوراً بدل انتظار الزيارات).
5. تحقق بعد النشر: `/admin/system` (Recent Errors)، `/robots.txt`، `/sitemap.xml`، صفحة وظيفة مرتين (الثانية من الكاش).
